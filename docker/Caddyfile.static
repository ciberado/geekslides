:5173

# Deck content (config.json, markdown, images, CSS) — served from mounted volume
handle /deck/* {
	uri strip_prefix /deck
	root * /srv/content
	file_server
}

# SPA fallback for app shell
handle {
	root * /srv/slides
	file_server
	try_files {path} /index.html
}
