{
  "name": "geekslides",
  "private": true,
  "type": "module",
  "description": "Geekslides is oriented towards creating effective remote presentations, specially for empowering learning experiences.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently --kill-others \"tsx packages/server/src/index.ts\" \"vite --host 0.0.0.0\"",
    "dev:nosync": "vite --host 0.0.0.0",
    "build": "npm run build -w @geekslides/engine && npm run build -w @geekslides/server && npm run build -w @geekslides/cli",
    "test": "vitest run",
    "test:e2e": "playwright test --config=e2e/playwright.config.ts",
    "lint": "eslint .",
    "typecheck": "tsc --build",
    "v1:install": "npm i --prefix broker && npm i --prefix demo && npm i --prefix slides",
    "v1:start": "demo/node_modules/concurrently/dist/bin/concurrently.js --kill-others \"npm --prefix broker run start\" \"npm --prefix slides run start\" "
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@typescript-eslint/eslint-plugin": "^8.18.0",
    "@typescript-eslint/parser": "^8.18.0",
    "@vitest/coverage-v8": "^3.0.0",
    "concurrently": "^9.2.1",
    "eslint": "^9.16.0",
    "jsdom": "^29.0.1",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.18.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ciberado/geekslides.git"
  },
  "author": "",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/ciberado/geekslides/issues"
  },
  "homepage": "https://github.com/ciberado/geekslides#readme"
}
