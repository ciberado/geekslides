{
  "name": "@geekslides/hub",
  "version": "2.1.0",
  "type": "module",
  "main": "src/server/index.ts",
  "types": "src/server/index.ts",
  "scripts": {
    "build": "npm run build:client && npm run build:server",
    "build:server": "esbuild src/server/index.ts --bundle --platform=node --format=cjs --outfile=dist/server/index.cjs --external:better-sqlite3 --external:pino --external:pino-pretty --external:thread-stream",
    "build:client": "vite build --config vite.client.config.ts",
    "typecheck": "tsc --noEmit",
    "start": "node dist/server/index.cjs",
    "dev": "concurrently --kill-others \"tsx src/server/index.ts\" \"vite --config vite.client.config.ts --host 0.0.0.0\"",
    "test:e2e": "playwright test --config e2e/playwright.config.ts"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.0",
    "@fastify/cors": "^10.0.0",
    "@fastify/jwt": "^10.0.0",
    "@fastify/multipart": "^9.0.0",
    "@fastify/static": "^9.1.3",
    "adm-zip": "^0.5.16",
    "better-sqlite3": "^11.7.0",
    "drizzle-orm": "^0.45.2",
    "fastify": "^5.2.0",
    "isomorphic-git": "^1.27.0",
    "pino": "^10.3.1",
    "pino-pretty": "^13.1.3"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.6",
    "@types/better-sqlite3": "^7.6.12",
    "esbuild": "^0.25.12",
    "lit": "^3.2.0",
    "vite": "^6.0.0"
  }
}
